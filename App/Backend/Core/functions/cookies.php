<?php defined("ACCESS") or exit("Access Denied");

// Cookies are set in loginController
// setcookie(COOKIE_NAME, bin2hex(random_bytes(16)), [
//     'expires' => time() + REMEMBER_ME_SESSION_LIFETIME,
//     'path' => "/",
//     'domain' => DOMAIN,
//     'secure' => true,                   // Ensures the cookie is only sent over HTTPS
//     'httponly' => true,                 // Ensures the cookie is not accessible by client-side scripts
//     'samesite' => SESSION_SAMESITE,     // Control cross-site cookie behavior
// ]);
